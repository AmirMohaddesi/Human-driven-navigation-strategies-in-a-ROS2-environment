# V3.6 runtime proof only: handoff node allowlist excludes llm_context.source_robot_id "robot1".
# Usage (after source install/setup.bash):
#   ros2 run multi_robot_mission_stack semantic-production-handoff-v35 --ros-args --params-file $(pwd)/scripts/params_semantic_handoff_v35_allowlist_other_only.yaml
semantic_production_handoff_v35:
  ros__parameters:
    allowed_source_robot_ids: ["other_robot"]
